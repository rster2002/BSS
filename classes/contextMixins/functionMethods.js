function functionMethods(instance) {
    return {
        functions: {},
        add(name, parameters, body) {

        },
        call(name, arguments) {
            
        }
    }
}

module.exports = functionMethods;